name: Hello World!

on:
  push:
    branches:
      - main

jobs:
  hello_world_job:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello World!"
      - name: Now it is
        run: date
      - name: Directory content
        run: ls -l